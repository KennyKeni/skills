interface:
  display_name: "Sidekick"
  short_description: "Keep frontier judgment while a sidekick executes"
  default_prompt: "Use $sidekick to run this coding task as a Fusion-style pair: keep the current session on judgment and final review, and use a persistent Luna Max sidekick for execution-heavy work."
policy:
  allow_implicit_invocation: false
