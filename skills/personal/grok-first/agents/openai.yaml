interface:
  display_name: "Grok First"
  short_description: "Delegate Claude implementation work to Grok"
  default_prompt: "Use $grok-first to delegate this implementation task through Cursor or OpenCode and Grok."
policy:
  allow_implicit_invocation: false
