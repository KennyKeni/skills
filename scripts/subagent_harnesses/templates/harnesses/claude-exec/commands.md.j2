{% macro verify() -%}
command claude --version
{%- endmacro %}

{% macro start(model_argument, effort_argument, permission_arguments) -%}
(cd "$REPO" \
  && CLAUDE_CODE_DISABLE_GIT_INSTRUCTIONS=1 command claude -p \
  --session-id "$SESSION_ID" \
  --model {{ model_argument }} \
  --effort {{ effort_argument }} \
  --output-format json \
  {{ permission_arguments }} \
  < "$PROMPT_FILE" > "$OUT" 2>/dev/null)
{%- endmacro %}

{% macro resume(model_argument, effort_argument, permission_arguments) -%}
(cd "$REPO" \
  && CLAUDE_CODE_DISABLE_GIT_INSTRUCTIONS=1 command claude -p \
  --resume "$SESSION_ID" \
  --model {{ model_argument }} \
  --effort {{ effort_argument }} \
  --output-format json \
  {{ permission_arguments }} \
  < "$PROMPT_FILE" > "$OUT" 2>/dev/null)
{%- endmacro %}

{% macro inspect_process() -%}
pgrep -fl -- "$SESSION_ID" || true
{%- endmacro %}
