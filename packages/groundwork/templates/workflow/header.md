# Workflow policy

This README is user-owned repository policy. Edit it directly when the policy
changes. Read live state — issues, labels, branches, pull requests, and
repository settings — from the forge and Git rather than treating this file as
cached state.

Forge: {{forgeLabel}} · Tracker: {{trackerLabel}} · Profile: {{profileLabel}}
